# ============================================================
# Luxury Retailer Inventory System — Environment Variables
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
# ============================================================

# ------------------------------------------------------------
# Database (PostgreSQL)
# Local development: matches docker-compose.yml defaults
# ------------------------------------------------------------
DATABASE_URL="postgresql://rolle:rolle_dev@localhost:5432/rolle_inventory"

# ------------------------------------------------------------
# Redis (BullMQ background jobs)
# Local development: matches docker-compose.yml defaults
# ------------------------------------------------------------
REDIS_URL="redis://localhost:6379"

# ------------------------------------------------------------
# NextAuth.js
# Generate NEXTAUTH_SECRET: openssl rand -base64 32
# ------------------------------------------------------------
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-openssl-rand-base64-32-output"

# ------------------------------------------------------------
# Email (Resend) — used for CRITICAL alert notifications
# Get your API key at https://resend.com
# ------------------------------------------------------------
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx"
EMAIL_FROM="inventory@yourdomain.com"
EMAIL_ALERT_RECIPIENTS="ops@yourdomain.com,coo@yourdomain.com"

# ------------------------------------------------------------
# Seed credentials (prisma/seed.ts only — never used at runtime)
# ------------------------------------------------------------
SEED_ADMIN_EMAIL="admin@example.com"
SEED_ADMIN_PASSWORD="change-me"

# ------------------------------------------------------------
# Application
# ------------------------------------------------------------
NEXT_PUBLIC_APP_NAME="Rolle Inventory"
NEXT_PUBLIC_BASE_CURRENCY="USD"

# ------------------------------------------------------------
# Inventory defaults (can be overridden per product/category)
# ------------------------------------------------------------
DEFAULT_SERVICE_LEVEL="0.95"          # 95% → Z = 1.65
DEFAULT_CARRYING_RATE="0.25"          # 25% annual carrying cost rate
DEFAULT_FORECAST_WEEKS_HISTORY="12"   # weeks of history for WMA forecast

# ------------------------------------------------------------
# Sentry error monitoring (optional)
# Create a project at https://sentry.io and paste the DSN here.
# Leave blank to disable Sentry (safe for local development).
# ------------------------------------------------------------
NEXT_PUBLIC_SENTRY_DSN=""
SENTRY_DSN=""
SENTRY_ORG=""
SENTRY_PROJECT=""
SENTRY_AUTH_TOKEN=""   # only needed for source map uploads during CI build
